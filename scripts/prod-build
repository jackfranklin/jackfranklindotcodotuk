echo "Run server in production on 3003 first!"

./node_modules/.bin/webpack --config client/webpack.production.config.js

./node_modules/.bin/babel-node ./scripts/build.js && exit 0

trap "trap - SIGTERM && kill -- -$$" SIGINT SIGTERM EXIT
